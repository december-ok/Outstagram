extends layouts/main.pug

block content
  .userControllPage
    .buttonGroup
      a(href=`/users${routes.editProfile}`).editProfileButton
        button= '✏️Edit Profile'
      a(href=`/users${routes.changePassword}`).changePassword
        button= '🔐Change Password'
      a(href=`/users${routes.removeAccount}`).removeAccount
        button= '🛑Remove Account🛑'